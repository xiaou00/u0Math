#分析 

>[!definition] 凸函数
>一个实[[映射|函数]]$f:(a,b)\to\R$, 若对于每个$x,y\in(a,b)$与每个$\lambda\in[0,1]$都有
>$$f(\lambda x+(1-\lambda)y)\le\lambda f(x)+(1-\lambda)f(y)$$
>或用几何表述: $(x,f(x))$与$(y,f(y))$之间的弦上每一点都位于图像上方, 则称$f$为一个**凸函数**(convex function).

