#代数几何 

>[!def] 正则嵌入
>设 $\pi:X\into Y$ 是[[局部闭嵌入]], 则我们称 $\pi$ 在点 $p\in X$ 是余维数为 $r$ 的**正则嵌入** (regular embedding), 当且仅当 $\mc O_{Y,p}$ 中, $X$ 所对的理想由一个长度 $r$ 的正则序列生成. 在每个点 $p\in X$ 都是余维数 $r$ 的正则嵌入, 则可直接称为余维数 $r$ 的正则嵌入.



