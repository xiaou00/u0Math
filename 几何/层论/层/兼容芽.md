#代数 

>[!def] 兼容芽
>设 $\mc F$ 是 $X$ 上的[[层]], $U\subset X$ 是开子集, 对于
>$$\prod_{p\in U}{s_p}\in\prod_{p\in U}{\mc F_p}$$
>若对于每个 $p\in U$ 都存在局部的代表元 $\tilde s_p\in\mc F(U_p)$ 对应于 $s_p$, 使得对所有 $q\in U_p$, $\tilde s_p$ 在 $q$ 处的芽恰好是 $s_q$, 则称该元素由**兼容芽**(compatible germs)组成.

