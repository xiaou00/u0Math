#代数 

>[!def] 极大理想
>设 $R$ 是一个[[环]], $M$ 是 $R$ 的一个[[理想]]且 $M\ne R$. 如果 $R$ 中包含 $M$ 的理想只有 $M$ 和 $R$ (即理想格中的极大元), 则称 $M$ 是 $R$ 的一个**极大理想**(maximal ideal).

>[!thm] 定理1
>设 $R$ 是有非零单位元的交换环, 则 $R$ 的理想 $M$ 为极大理想当且仅当 $R/M$ 是[[域]].

