#代数 

多项式代数的构造是非常简单的, 它可以构造为自由幺半群的[[幺半群环]], 也就是自由函子 (遗忘函子的[[伴随函子]]) 和幺半群环构造函子的复合:

$$\mathsf{Set}\xrightarrow{\mathrm{Free}}\mathsf{Mon}\to \mathsf{Alg}_R$$

或者也可以构造性地定义如下:

>[!def] 多项式代数
>集合 $X$ 生成的 $R$-多项式代数 $R[X]$ 是如下集合
>$$\set{\sum{rx^n}\mid r\in R,x\in X,n\in\N}$$
>对于自然的运算构成的[[代数]].



