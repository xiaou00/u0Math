#交换代数 

>[!def] p-adic赋值
>设 $\Spec~\Z$ 是算术[[概形]]. p-adic赋值就是 $\Spec~\Z$ 上的[[零点和极点]]赋值, 对于素数 $p$, 定义 $v_p:\Q^\times\to\Z$ 为[[赋值]] $r/s\mapsto v_p(r)-v_p(s)$, 这里的 $v_p$ 是素数在素因数分解里出现的次数. 这个赋值被称作**p-adic赋值** (p-adic valuation).

